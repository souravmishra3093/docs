---

title: "Transliteration API"
api: "POST https://revapi.reverieinc.com/"
description: "The Transliteration API enables you to convert text content from one language script to another. Below are examples demonstrating how to transliterate single and multiple sentences."
playground: "none"

---

 <RequestExample>
 ```bash cURL
 curl --location --request POST 'https://revapi.reverieinc.com/localization' \
 --header 'Content-Type: application/json' \
 --header 'REV-API-KEY: <YOUR API KEY>' \
 --header 'REV-APP-ID: <YOUR APP-ID>' \
 --header 'src_lang: en' \
 --header 'tgt_lang: hi' \
 --header 'domain: generic' \
 --header 'REV-APPNAME: localization' \
 --header 'REV-APPVERSION: 3.0' \
 --data-raw '{
 "data": ["Hello, how are you?"],
 "nmtMask": true,
 "enableNmt": true,
 "enableLookup": false
 }'
 ```
 ```python Python
 import requests
 url = "https://api.example.com/endpoint"
 payload = {"key": "value"}
 headers = {"Content-Type": "application/json"}
 response = requests.post(url, json=payload, headers=headers)
 print(response.text)
 ```
 ```javascript JavaScript
 fetch('https://api.example.com/endpoint', {
 method: 'POST',
 headers: {
 'Content-Type': 'application/json'
 },
 body: JSON.stringify({
 key: 'value'
 })
 })
 .then(response => response.json())
 .then(data => console.log(data));
 ```
 </RequestExample>
 
 
 ### Headers
 
 <ParamField header="Content-Type" type="string" required>
   {" "}
   The format of the data to be posted.
 </ParamField> <ParamField header="REV-API-KEY" type="string" required>
   {" "}
   A unique key/token provided by Reverie to identify the user using the Localization
   API.{" "}
 </ParamField> <ParamField header="REV-APP-ID" type="string" required>
   {" "}
   The unique account ID to identify the user and the default account settings.
 
 {" "}
 
 </ParamField> <ParamField header="src_lang" type="string" required>
   {" "}
   Source language code (e.g., `en` for English).{" "}
 </ParamField> 
 <ParamField header="tgt_lang" type="string" required>
   {" "}
   Target language code (e.g., `hi` for Hindi).{" "}
 </ParamField> <ParamField header="domain" type="string" required>
   {" "}
   Domain category (e.g., `generic`).{" "}
 </ParamField> <ParamField header="cnt_lang" type="string" required>
   {" "}
   Content language code (e.g., `en` for English).{" "}
 </ParamField> <ParamField header="REV-APPNAME" type="string" required>
   {" "}
   Application name (e.g., `transliteration`).{" "}
 </ParamField> <ParamField header="REV-APPVERSION" type="string" required>
   {" "}
   Application version (e.g., `2.0`).{" "}
 </ParamField>
 
 ### Body
 
 <ParamField body="data" type="array" required>
   {" "}
   An array containing the text strings to be transliterated.{" "}
 </ParamField> <ParamField body="isBulk" type="boolean" required>
   {" "}
   Indicates whether the request is for bulk processing. Set to `false` for single
   sentence transliteration.{" "}
 </ParamField> <ParamField body="ignoreTaggedEntities" type="boolean" required>
   {" "}
   Determines whether to ignore tagged entities during transliteration. Set to `false`
   to include all entities.{" "}
 </ParamField>
 
 <ResponseExample>
 ```bash 200
 {
 	"responseList": [
     	{
         	"inString": "Reverie Language Technologies ltd website address is www.reverieinc.com ",
         	"outString": [
             	"रेवरी लैंग्वेज टेक्नोलॉजीस लिमिटेड वेबसाइट एड्रेस इज़ www.reverieinc.com"
         	],
         	"status": 2
     	}
    ]
 }
 ```
 ```bash 400
 {
 	"message" : "Incorrect Data"
 }
 ```
 
 
 </ResponseExample>
